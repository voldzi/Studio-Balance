packages:
  - apps/*
  - packages/*

allowBuilds:
  esbuild: true
  unrs-resolver: true

overrides:
  fastify: 5.12.3
  find-my-way: 9.9.0
  # PostCSS still pins an affected nanoid 3 patch; remove after upstream updates.
  'nanoid@>=3.0.0 <3.3.18': 3.3.18
